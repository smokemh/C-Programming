/* Write a program in C to display the n terms of odd natural number and their sum. Go to the editor*/

#include <stdio.h>
void main()
{
    int num, y;
    printf("Enter The num of times you want to print using loop = ");
    scanf("%d", &num);
    y = 0;
    while (y < num)
    {
        printf("Hello World\n");

        y++;
    }
}
